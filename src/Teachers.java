import java.util.Scanner;

public class Teachers extends Users {
    Scanner input = new Scanner(System.in);

    // Show the administrator their permission

    // Collect the users choice

    // Dealing with the users choices

}
